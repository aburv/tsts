export type GAuthUser = {
  sub: string;
  email: string;
  name: string;
  picture: string;
  firstName: string;
  lastName: string;
}

export type AppUser = {
  dp: string, name: string, email: string
}