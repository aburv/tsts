export interface GAuthUser {
  sub: string;
  email: string;
  name: string;
  picture: string;
  firstName: string;
  lastName: string;
}

export interface AppUser {
  dp: string, name: string, email: string
}